# UX 增强验证清单

> 运行 `python -m src.main` 后逐项检查。

---

## 1. 状态信息（左上角）
- [ ] 左上角显示 **"天体: 2 | 倍速: 1× | FPS: 60"**: 汉字显示有误 改为英文
- [x] FPS 数值随帧率变化
- [x] 暂停时倍速显示不受影响
- [ ] 鼠标移动时第二行显示 **"鼠标: (1.23e11, 4.56e10) m"** 并没有，保持为0
- [ ] 坐标值随鼠标实时更新 并未

## 2. 比例尺（右下角）
- [x] 右下角显示一条水平比例尺条
- [x] 比例尺两端有短竖线标记
- [ ] 比例尺上方有文字（如 "2 亿 km" / "800 百万 km"） 文字显示有误，例如2 后面显示两个方框 不要使用中文，且使用科学计数法。而且比例尺被 "Space: Pause; R:reset; Esc:Menu" 一行遮挡。
- [x] 缩放时比例尺长度和文字同步更新
- [x] 数值取整到 1/2/5 的倍数（不是随意数字）

## 3. 坐标网格（G 键）
- [x] 按 `G` → 出现网格线 没反应 或是看不见
- [x] 再按 `G` → 网格消失
- [ ] 缩放到很小时（zoom < 0.01）网格间距变大 网格间距未随缩放变化
- [ ] 缩放到很大时（zoom > 10）网格间距变小
- [x] 平移时网格跟随视野移动
- [x] 网格线为半透明暗色，不遮挡天体

## 4. 天体标签（L 键）
- [x] 按 `L` → 天体上方出现标签（"Star #0"、"Planet #1"）
- [x] 再按 `L` → 标签消失
- [x] 恒星标签为金色文字
- [x] 行星标签为蓝色文字
- [x] 标签有半透明黑色背景
- [x] 放置新天体后标签立即显示

## 5. 快捷键面板（H 键）
- [x] 按 `H` → 出现半透明覆盖层
- [ ] 面板居中，标题 "快捷键 (按 H 或 Esc 关闭)" 中文有问题（ 改成英文
- [x] 两列布局，键位和功能描述对齐
- [x] 按 `H` → 面板关闭
- [x] 按 `Esc` → 面板关闭
- [x] 面板打开时游戏仍在后台运行（不暂停）

## 6. 快捷键变更
- [x] 按 `G` → 切换网格（不是 4× 加速）
- [x] 按 `L` → 切换标签（新增）
- [x] 按 `H` → 切换快捷键面板（不是 8× 加速）
- [x] 按 `6` → 4× 加速 不妨改为7
- [x] 按 `7` → 8× 加速 不妨改为8
- [x] 按 `F` → 2× 加速（不变）改为 6
- [x] 按 `0` → 恢复 1×（不变） 改为 5

## 7. 平滑相机跟随
- [x] 双击天体 → 进入参考系模式
- [ ] 相机从当前位置**平滑过渡**到目标天体（不是瞬移） 就是（或者说视觉上）顺移过去的
- [x] 目标天体运动时，相机有**阻尼跟随**效果（不是完全锁定） 功能无问题，但额外增加：当处于非默认参考系时，缩放不与鼠标位置相关，默认中心。
- [ ] 按 `R` → 重置相机仍然是瞬移（`follow()` 不应受影响） 这是什么功能？去掉（
- [x] 按 `Esc` 退出参考系 → 回到之前视角

## 8. 回归测试
- [x] 左键拖拽天体正常（暂停 + 跟随鼠标）
- [x] 放置行星（按 2 → 点击 → 拖拽）正常
- [x] 右键探测器瞄准 + 发射正常
- [x] 双击 → 参考系 + 自动缩放正常
- [x] 时间控制（暂停/F/6/7/0）正常
- [x] 右键编辑天体弹窗正常
- [ ] 尾迹（T 键切换）正常 这是什么功能（？ 去掉

---

## 测试命令

```bash
# 运行自动化测试
python -m pytest tests/ -q --ignore=tests/test_physics.py

# 预期：76 passed
```
